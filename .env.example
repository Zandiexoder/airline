# Example Environment Variables for Airline Docker Setup
# Copy this file to .env and customize as needed
# Note: docker-compose.override.yaml takes precedence for container-specific settings

# Database Configuration
# DB_HOST=airline-db
# DB_NAME=airline
# DB_USER=mfc01
# DB_PASSWORD=ghEtmwBdnXYBQH4

# Elasticsearch Configuration
# ES_HOST=airline-search

# Google Maps API (required for map features)
# Add your key in airline-web/conf/application.conf instead

# Notes:
# - These variables can be set in docker-compose.override.yaml
# - Default values are already configured in the containers
# - For production, use Docker secrets or a proper secrets manager
